
  <h4><%=t('Comm Categories') %></h4>
  <table class="table table-striped">
    <tr>
      <th>#</th>
      <th><%=t('Create Date') %></th>
      <th><%=t('Comm Category') %></th>
      <th><%=t('Brief Note') %></th>
      <th><%=t('Ranking Order') %></th>
      <th><%=t('Entered By') %></th>
      <th></th>
    </tr>

    <% @misc_definitions.each do |r| %>
        <tr>
          <td><%= r.id %></td>
          <td><%= (r.created_at + 8.hours).strftime("%Y/%m/%d")%></td>
          <td><%= r.name %></td>
          <td><%= r.brief_note %></td>
          <td><%= r.ranking_order %></td>
          <td><%= r.last_updated_by.name %></td>
          <td><%= link_to t('Edit'), edit_misc_definition_path(r, :for_which => 'customer_comm_category', :subaction => 'customer_comm_category', :format => :js), 
                   :remote => true, :class => BUTTONS_CLS['action'] if has_action_right?('update_customer_comm_category', 'customerx_misc_definitions', r)  %></td>
        </tr>
    <% end %>

  </table>

<table class="table table-striped">
  <tr>
    <th>
    	&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
		<%= link_to t("New Comm Category"), new_misc_definition_path(:for_which => 'customer_comm_category', :subaction => 'customer_comm_category', :format => :js), :remote => true, 
              :id => 'new_log_link', :class => BUTTONS_CLS['action']  if has_action_right?('create_customer_comm_category', 'customerx_misc_definitions') %>
    </th>
  </tr>
</table>
  <%= link_to t('Back'), SUBURI + "/authentify/view_handler?index=0", :id => 'close' , :class => BUTTONS_CLS['action']%>
