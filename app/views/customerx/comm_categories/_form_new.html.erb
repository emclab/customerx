<%= simple_form_for @comm_category do |f| %>    
 
  <%= f.input :name, :label => "Comm Category:" %>  
  <%= f.input :brief_note, :label => "Brief Note:" %>  
  <%= f.input :ranking_order, :label => 'Ranking Index', :placehoder => 'ex, 1, 2, 3 ...'%> 
  <%= f.button :submit, 'Save' %>  
<% end %> 